Senha: validadores nativos do Django.
CPF: validate-docbr.
Placa de veículo: re.
Telefone: re.

Serializer

Validações relacionadas à API:

verificar se um e-mail já existe
verificar se uma placa já está cadastrada
validar campos em conjunto
Validators

Validações reutilizáveis:

CPF válido
telefone válido
placa no formato correto
Model

Regras de negócio:

vaga não pode ficar ocupada por dois carros
data de saída não pode ser menor que a entrada